namespace FesajoStudios.Server.Extensions
{
    public static class TheatherConversions
    {
        //public static IEnumerable<ProductoDto> ConvertToDto(this IEnumerable<Product> products, IEnumerable<ProductCategory> productCategories)
        //{
        //    return (from product in products
        //            join productCategory in productCategories
        //            on product.CategoryId equals productCategory.Id
        //            select new ProductoDto
        //            {
        //                Id = product.Id,
        //                Name = product.Name,
        //                Description = product.Description,
        //                ImageURL = product.ImageURL,
        //                Price = product.Price,
        //                Qty = product.Qty,
        //                CategoryId = product.CategoryId,
        //                CategoryName = productCategory.Name

        //            }).ToList();

        //}
    }
}
